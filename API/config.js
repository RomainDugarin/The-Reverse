var config = {};

config.port = 9000;

//Authentication
config.auth = false;

//Database
config.database = 'the-reverse';
config.username = 'invite';
config.password = 'invite';
config.table_prefix = '';

//Pagination
config.paginate = false;
config.page_limit = 500;

module.exports = config;